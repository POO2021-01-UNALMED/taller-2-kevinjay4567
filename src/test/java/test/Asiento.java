package test;

public class Asiento {
    String color;
    int precio;
    int registro;

    public void cambiarColor(String colorcambio){
    if (colorcambio.equals("blanco") || colorcambio.equals("rojo") || colorcambio.equals("verde") || colorcambio.equals("amarillo") || colorcambio.equals("negro")) {
        color = colorcambio;
    }
        }
            }
